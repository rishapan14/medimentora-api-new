blinker==1.9.0
cffi==2.0.0
click==8.4.1
cryptography==48.0.0
Flask==3.1.3
flask-cors==6.0.2
Flask-JWT-Extended==4.7.4
Flask-SQLAlchemy==3.1.1
greenlet==3.5.1
gunicorn==26.0.0
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3

# ---------------------------------------------------------------------------
# OCR / Computer Vision stack (Medical Report Analysis)
# ---------------------------------------------------------------------------
# Core image processing (installed & verified on this machine)
numpy==2.5.1
# Headless is the server/container build; 5.0.0 (without the build suffix)
# does not exist on PyPI and caused the deployment's dependency resolution error.
opencv-python-headless==5.0.0.93
pillow==12.3.0

# DICOM radiograph validation / pixel extraction (Phase 1 X-ray uploads)
pydicom==3.0.1

# PDF page rendering
# pdf2image requires Poppler system binaries for convert_from_path().
# PyMuPDF (pymupdf) is the primary PDF renderer — no Poppler needed.
pdf2image==1.17.0
pymupdf==1.28.0
pypdf==5.8.0

# PaddleOCR runtime used TODAY (ONNX — works on Python 3.10–3.14, no GPU needed)
# This loads PaddleOCR detection/recognition models via ONNX Runtime.
rapidocr-onnxruntime==1.2.3

# Full PaddlePaddle + PaddleOCR (preferred when Python supports it)
# Official paddlepaddle wheels currently support Python 3.9–3.12 only.
# On Python 3.14 they cannot be installed — RapidOCR above is the fallback.
# Uncomment / install inside a Python 3.10–3.12 virtualenv:
#   paddlepaddle==3.0.0
#   paddleocr==2.9.1
# paddlepaddle>=2.6.0,<3.1.0
# paddleocr>=2.7.0,<3.0.0

# Legacy optional OCR (kept for fallback; not used when RapidOCR/PaddleOCR available)
pytesseract==0.3.13

# ---------------------------------------------------------------------------
# AI
# ---------------------------------------------------------------------------
openai==1.95.0
# Gemini for AI Medical Teacher enrichment (optional; heuristic parser works without it)
google-generativeai==0.8.5

# Document processing (AI Medical Teacher — Module 1)
python-docx==1.2.0

# ---------------------------------------------------------------------------
# App utilities
# ---------------------------------------------------------------------------
packaging==26.2
pycparser==3.0
PyMySQL==1.2.0
python-dotenv==1.2.2
reportlab==4.4.2
SQLAlchemy==2.0.50
typing_extensions==4.15.0
Werkzeug==3.1.8

# ---------------------------------------------------------------------------
# Testing (Module 11 — X-ray analysis)
# ---------------------------------------------------------------------------
pytest==8.4.1
