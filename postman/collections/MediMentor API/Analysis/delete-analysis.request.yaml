$kind: http-request
name: 'Delete Analysis'
method: DELETE
url: '{{baseUrl}}/api/analysis/{{analysisId}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
