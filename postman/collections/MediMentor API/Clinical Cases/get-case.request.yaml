$kind: http-request
name: 'Get Clinical Case'
method: GET
url: '{{baseUrl}}/api/clinical-cases/{{caseId}}'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
