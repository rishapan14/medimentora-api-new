$kind: http-request
name: 'List Favorite Cases'
method: GET
url: '{{baseUrl}}/api/clinical-cases/favorites'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
