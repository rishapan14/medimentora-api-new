$kind: http-request
name: 'Extract Text from Report'
method: POST
url: '{{baseUrl}}/api/reports/{{reportId}}/extract'
order: 7000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
