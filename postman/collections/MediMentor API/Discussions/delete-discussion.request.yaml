$kind: http-request
name: 'Delete Discussion'
method: DELETE
url: '{{baseUrl}}/api/discussions/{{discussionId}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
