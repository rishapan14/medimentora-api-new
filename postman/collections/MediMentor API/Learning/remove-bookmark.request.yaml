$kind: http-request
name: 'Remove Bookmark'
method: DELETE
url: '{{baseUrl}}/api/learning/lessons/{{lessonId}}/bookmark'
order: 14000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
