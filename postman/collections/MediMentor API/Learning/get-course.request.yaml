$kind: http-request
name: 'Get Course'
method: GET
url: '{{baseUrl}}/api/learning/courses/{{courseId}}'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
